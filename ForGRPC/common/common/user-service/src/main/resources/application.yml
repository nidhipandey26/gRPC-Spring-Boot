grpc:
  server:
    port: 9002

#server:
#  port: 2121

spring:
  application:
    name: USER-SERVICE
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/test
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: create
      show-sql: true